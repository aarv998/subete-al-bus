import React from "react";
import ReactDOM from "react-dom/client";
import SubeteAlBus from "./App";
import "./App.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
    <React.StrictMode>
        <SubeteAlBus />
    </React.StrictMode>
);
